FROM node:22-alpine AS dependencies

RUN apk add --no-cache libc6-compat openssl python3 make g++
WORKDIR /app
COPY package.json package-lock.json ./
COPY apps/whatsapp-reader/package.json ./apps/whatsapp-reader/package.json
RUN HUSKY=0 npm ci --workspace=@superflash/whatsapp-reader --include-workspace-root

FROM dependencies AS builder
COPY tsconfig.base.json ./tsconfig.base.json
COPY apps/whatsapp-reader ./apps/whatsapp-reader
RUN npm run build --workspace=@superflash/whatsapp-reader

FROM node:22-alpine AS runner
RUN addgroup -S app && adduser -S app -G app
WORKDIR /app
ENV NODE_ENV=production
COPY --from=dependencies --chown=app:app /app/node_modules ./node_modules
COPY --from=dependencies --chown=app:app /app/package.json ./package.json
COPY --from=dependencies --chown=app:app /app/apps/whatsapp-reader/package.json ./apps/whatsapp-reader/package.json
COPY --from=builder --chown=app:app /app/apps/whatsapp-reader/dist ./apps/whatsapp-reader/dist
RUN mkdir -p /data/auth && chown -R app:app /data
USER app
HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=5 CMD wget --spider --quiet http://127.0.0.1:3010/health || exit 1
CMD ["node", "apps/whatsapp-reader/dist/index.js"]
