# ADR-024 — WhatsApp Web Read-Only Source

Usar un servicio `whatsapp-reader` aislado con Baileys y un contrato interno normalizado. El API es la única puerta al read model y el servicio no puede escribir en el canal externo.
